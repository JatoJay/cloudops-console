{{- if not .Values.pairingToken }}{{ fail "pairingToken is required" }}{{ end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cloudops-agent.fullname" . }}
type: Opaque
stringData:
  pairing-token: {{ .Values.pairingToken | quote }}
